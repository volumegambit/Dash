{
  "name": "dash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/agent",
    "packages/channels",
    "packages/chat",
    "packages/logging",
    "packages/management",
    "packages/mc",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/agent -w packages/channels -w packages/logging -w packages/management -w packages/chat -w packages/mc -w apps/dash -w apps/gateway -w apps/tui -w apps/mc-cli",
    "dev": "npm run dev --workspace=apps/dash",
    "tui": "npm run dev --workspace=apps/tui",
    "mc-cli": "node --import tsx apps/mc-cli/src/index.ts",
    "gateway": "node --import tsx apps/gateway/src/index.ts",
    "mc:dev": "npm run dev --workspace=apps/mission-control",
    "mc:dev:debug": "npm run dev:debug --workspace=apps/mission-control",
    "mc:build": "npm run build --workspace=apps/mission-control",
    "mc:preview": "npm run build --workspace=apps/mission-control && npm run preview --workspace=apps/mission-control",
    "mc:package": "npm run build --workspace=apps/mission-control && npm run package --workspace=apps/mission-control",
    "website:dev": "npm run dev --workspace=apps/website",
    "website:build": "npm run build --workspace=apps/website",
    "test": "vitest run",
    "lint": "biome check . && npm run typecheck --workspace=apps/mission-control",
    "lint:fix": "biome check --write .",
    "clean": "rm -rf packages/*/dist apps/*/dist apps/*/out",
    "version:sync": "V=$(node -p \"require('./package.json').version\") && for f in packages/*/package.json apps/*/package.json; do node -e \"const fs=require('fs');const p=JSON.parse(fs.readFileSync('$f'));p.version='$V';fs.writeFileSync('$f',JSON.stringify(p,null,2)+'\\n')\"; done"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
