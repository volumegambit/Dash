package app.dash.designsystem

import androidx.compose.ui.graphics.Color

/**
 * Palette mirrored from Mission Control
 * (apps/mission-control/src/renderer/src/assets/main.css) so the phone app
 * matches the desktop. Dark theme only in v1.
 */
val Background = Color(0xFF0A0A0A)
val Surface = Color(0xFF111111)
val SurfaceCard = Color(0xFF141414)
val BorderColor = Color(0xFF262626)
val Foreground = Color(0xFFF8FAFC)
val Muted = Color(0xFF94A3B8)
val Accent = Color(0xFF2563EB)
val AccentHover = Color(0xFF1D4FBF)

/** Agent-status indicators. */
val StatusActive = Color(0xFF22C55E)
val StatusDisabled = Color(0xFF94A3B8)
val StatusRegistered = Color(0xFFFACC15)

val ErrorRed = Color(0xFFF87171)
